apiVersion: v1
kind: Pod
metadata:
  name: {{ .Chart.Name }}-{{ .Release.Namespace }}-1
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  containers:
    - name: busybox
      image: busybox
      resources:
        limits:
          memory: "128Mi"
          cpu: "250m"
      stdin: true
---
apiVersion: v1
kind: Pod
metadata:
  name: {{ .Chart.Name }}-env-szvi26-1
  namespace: env-szvi26
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  containers:
    - name: busybox
      image: busybox
      resources:
        limits:
          memory: "128Mi"
          cpu: "250m"
      stdin: true
